# 프로젝트 공통 지침

## 제품 맥락

- Calio는 AI 기반 일정 관리 캘린더 시스템이다.
- 주요 사용자 경험은 iOS 앱에서 제공한다.
- 비즈니스 상태와 API 계약의 의미는 backend가 소유한다.
- 사용자에게 보이는 상태, 메시지와 처리 결과는 backend와 frontend에서 일관된 의미를 가져야 한다.

## 개발 지침 적용

- 코드 변경 전에 작업 범위를 backend, frontend, infra로 구분한다.
- CI/CD Workflow, Dockerfile과 배포 설정 변경은 파일 위치와 관계없이 Infra 작업 범위로 분류한다.
- 변경 대상 stack의 `AGENTS.md`를 확인한 후 구현을 시작한다.
- 여러 stack을 변경하면 각 stack의 `AGENTS.md`를 모두 확인한다.
- 각 stack의 `AGENTS.md`가 지정한 상세 개발 문서 중 변경과 관련된 문서를 구현 전에 확인한다.
- 문서의 지침과 기존 코드 구조가 일치하지 않으면 임의로 판단하지 않고 차이와 영향 범위를 먼저 설명한다.

## 공통 계약 원칙

- Backend는 비즈니스 상태, 정책 판단과 API 계약의 source of truth다.
- Frontend는 backend가 제공한 canonical field를 우선 사용하고 같은 비즈니스 의미를 다시 계산하지 않는다.
- 사용자에게 보이는 문구와 UI 표현은 backend 계약과 충돌하지 않아야 한다.
- Shared literal, enum, status, error code와 canonical vocabulary는 contract-sensitive하게 다룬다.
- 닫힌 계약에 새로운 값이 들어왔을 때 기존 값으로 조용히 변환하거나 의미를 임의로 넓히지 않는다.
- 외부에 공개되거나 다른 stack이 소비하는 계약은 명시적인 변경 없이 호환성을 깨지 않는다.

## 도메인 언어

- 제품과 API에서 사용하는 canonical domain terminology는 `docs/domain/glossary.md`를 기준으로 한다.
- 용어 구분이 중요한 영역을 변경할 때는 관련 용어 정의를 확인한다.
- 새로운 용어를 추가하거나 기존 의미를 변경할 때는 backend, frontend와 사용자 노출 표현의 일관성을 함께 검토한다.

## 테스트 원칙

- 테스트는 기대 동작과 계약을 검증하는 실행 가능한 명세로 취급한다.
- 동작을 변경하기 전에 관련 테스트를 확인하여 기존에 보장하던 동작과 경계 조건을 파악한다.
- 새로운 계약, 상태 전이, 정렬 의미와 사용자 노출 동작은 가능한 경우 자동 테스트로 고정한다.
- 테스트는 구현 세부보다 기대 동작, 비즈니스 경계 조건과 실패 동작을 설명하고, 테스트 이름과 구조만으로 그 의미를 이해할 수 있어야 한다.
- 기대 동작을 의도적으로 변경할 때는 구현과 테스트를 함께 변경하여 명세와 실제 동작을 일치시킨다.
- Stack별 테스트 작성 방식과 검증 명령은 해당 디렉터리의 `AGENTS.md`와 상세 개발 지침을 따른다.
