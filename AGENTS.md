# 프로젝트 공통 가이드

## 제품 맥락

- 이 프로젝트는 AI 일정 관리 캘린더 시스템이다.
- 주요 사용자 경험은 iOS 앱에서 제공하고, 비즈니스 상태와 계약 의미는 backend가 소유한다.
- 사용자에게 보이는 상태, 메시지, 처리 결과는 backend와 frontend에서 의미가 일치해야 한다.

## 공통 설계 원칙

- 기존 구조를 우선 확장하고 불필요한 새 레이어 추가를 피한다.
- 기존 계약을 깨는 변경보다 부가적인 변화를 우선한다.
- 비즈니스 의미는 한 곳에서만 소유하도록 유지한다.
- 닫힌 계약은 조용히 넓히지 않고, 불일치가 있으면 명시적으로 드러낸다.
- 변경은 가능한 한 작은 단위로 나누고, 도메인 경계를 흐리지 않는다.

## 도메인 간 책임

- backend는 상태 의미, 정책 판단, API 계약의 단일 진실 공급원이다.
- frontend iOS 앱은 backend가 제공한 canonical field를 우선 사용하고 의미를 다시 계산하지 않는다.
- 사용자에게 보이는 문구와 UI 표현은 backend 계약과 충돌하지 않아야 한다.
- shared literal, enum, canonical vocabulary는 contract-sensitive 하다.

## 공통 용어

- `event`: 캘린더에 표시되는 기본 일정 단위다. 독립 일정과 반복 일정의 개별 회차를 포괄한다.
- `recurrence-event`: 반복 규칙을 가지며 여러 개의 반복 회차를 생성하는 일정 시리즈다. 개별 회차 자체를 의미하지 않는다.
- `recurrence-occurrence`: `recurrence-event`에 속하는 개별 일정 회차다.
- `recurrence-event-override`: 특정 `recurrence-occurrence`의 시간이나 내용을 변경하거나 해당 회차를 제외한 상태다.
- `all-day-event`: 특정 시각이 아니라 하나 이상의 날짜 전체를 차지하는 일정이다.
- `conversation`: 하나의 계정이 AI 캘린더 도우미와 이어가는 대화 단위다. 여러 `message`를 시간순으로 포함하며, 계정 소유권과 대화 활동 시각을 가진다.
- `message`: `conversation` 안에 기록되는 한 건의 사용자 또는 AI 캘린더 도우미 발화다. 역할(`USER` 또는 `ASSISTANT`)과 본문을 가진다.

## 테스트 원칙

- 가능한 모든 변경에는 자동 테스트를 추가하는 방향을 우선한다.
- 테스트는 단순 회귀 방지 수단이 아니라, 시스템 동작을 설명하는 실행 가능한 명세서여야 한다.
- 테스트만 읽어도 기대 동작, 경계 조건, 실패 동작을 이해할 수 있어야 한다.
- 의미가 중요한 로직은 구현 세부보다 기대 동작 중심으로 테스트를 작성한다.
- 새로운 계약, 상태 전이, 정렬 의미, 사용자 노출 동작이 추가되면 그 의미를 테스트로 고정한다.
- 테스트는 읽기 쉬운 이름, 명확한 준비 단계, 분명한 기대 결과를 가져야 한다.
- 불필요하게 복잡한 fixture나 과도한 mocking보다, 동작 이해에 도움이 되는 구조를 우선한다.
